# Capstone-yohan-front 변경사항 정리

> 기준일: 2026-05-18
> Capstone 폴더의 최신 코드를 yohan-front에 동기화한 내역

---

## API 파일

| 파일 | 변경 내용 |
|------|-----------|
| `api/studentLecture.ts` | Capstone에서 통째로 복사 (전면 변경) |
| `api/attendance.ts` | Capstone에서 통째로 복사 |
| `api/auth.ts` | Capstone에서 통째로 복사 |

---

## 상수 / 유틸 / Hooks

| 파일 | 변경 내용 |
|------|-----------|
| `constants/semester.ts` | `CURRENT_SEMESTER_NUM`을 `"1학기"`/`"2학기"` 형태로 변경, `SEMESTER_RAW` 추가, `getSemesterStartDate` 함수 추가 |
| `utils/format.ts` | 신규 추가 (`formatPhone` 유틸) |
| `hooks/useAuth.ts` | Capstone에서 복사 |

---

## 컴포넌트

| 파일 | 변경 내용 |
|------|-----------|
| `components/NotificationBell.tsx` | `mapRedirectUrl` 추가 — admin role의 "mypage" URL을 `/admin/photo-requests`로 라우팅 |
| `components/FormModal.tsx` | Capstone에서 복사 (Dialog 컴포넌트 기반으로 변경) |

---

## 학생 페이지

| 파일 | 변경 내용 |
|------|-----------|
| `pages/student/StudentHome.tsx` | 시간표 7일(토/일 포함), 14시간(08~21), 아이콘 기반 출결 표시로 변경 |
| `pages/student/StudentTimetable.tsx` | 7일/14슬롯 확장, 그리드 7열 |
| `pages/student/StudentCourseStats.tsx` | 학기 포맷 변경, 주차/교시 그룹 표시 방식으로 개선 |
| `pages/student/StudentStats.tsx` | Capstone에서 통째로 복사 (전면 개편) |
| `pages/student/StudentSignup.tsx` | Capstone에서 통째로 복사 (전면 개편) |
| `pages/student/StudentCourseDetail.tsx` | Capstone에서 통째로 복사 (전면 변경) |
| `pages/student/StudentCourses.tsx` | Capstone에서 통째로 복사 |
| `pages/student/StudentProfile.tsx` | Capstone에서 통째로 복사 (학과 제거, 전화번호 배치 변경, 얼굴인식 추가) |
| `pages/student/StudentAbsenceRequest.tsx` | Capstone에서 복사 |
| `pages/student/StudentCourseDetailPage.tsx` | Capstone에서 복사 |
| `pages/student/StudentDashboard.tsx` | `userDepartment` 하드코딩 제거 |
| `pages/Login.tsx` | Capstone에서 통째로 복사 (전면 변경) |
| `pages/PasswordReset.tsx` | Capstone에서 통째로 복사 (전면 변경) |

---

## 교수 페이지

| 파일 | 변경 내용 |
|------|-----------|
| `pages/professor/ProfessorClassControl.tsx` | Capstone에서 통째로 복사 (Dialog 기반 모달) |
| `pages/professor/ProfessorProfile.tsx` | Capstone에서 통째로 복사 (Dialog 기반 비밀번호 모달) |
| `pages/professor/ProfessorCourseQA.tsx` | Capstone에서 통째로 복사 (타입 안전 버전) |
| `pages/professor/ProfessorSignup.tsx` | 인라인 `formatPhone` 제거 → `utils/format`에서 import |
| `pages/professor/ProfessorAbsenceManagement.tsx` | `AbsenceRequest` 타입 import 추가 (`any` → 정확한 타입) |
| `pages/professor/ProfessorAppealManagement.tsx` | `AppealRequest` 타입 import 추가 (`any` → 정확한 타입) |
| `pages/professor/ProfessorAttendanceEdit.tsx` | 에러 메시지 개선 ("로드 실패" → "출결 데이터를 불러오지 못했습니다.") |
| `pages/professor/ProfessorCourseAttendance.tsx` | 에러 메시지 개선 |
| `pages/professor/ProfessorCourseDetail.tsx` | 에러 메시지 개선 + `maxWidth` 반응형 수정 |

---

## 관리자 페이지

| 파일 | 변경 내용 |
|------|-----------|
| `pages/admin/AdminCourseManagement.tsx` | Capstone에서 통째로 복사 (다중 요일 → 단일 요일, 시간 UI에 `~` 추가) |
| `pages/admin/AdminProfessorManagement.tsx` | 인라인 `formatPhone` 제거 → `utils/format`에서 import |
| `pages/admin/AdminStudentManagement.tsx` | 인라인 `formatPhone` 제거 → `utils/format`에서 import |
| `pages/admin/AdminStudentTable.tsx` | `formatPhone` → `utils/format` import + `formatPhoneDisplay` 래퍼 패턴 적용 |
| `pages/admin/AdminHome.tsx` | 변경 없음 (yohan-front 버전이 이미 최신 — 실제 API 연동) |
| `pages/admin/AdminDeviceManagement.tsx` | 변경 없음 (yohan-front 버전이 이미 최신 — 실제 API 연동) |

---

## 변경 없음 (yohan-front가 이미 최신이었던 파일)

- `hooks/useClassSimulator.tsx` — 실제 API 연동 버전
- `pages/professor/ProfessorHome.tsx` — 실제 API 연동 버전
- `pages/professor/ProfessorCourses.tsx` — 에러 UI 포함 버전
- `pages/shared/NotificationsPage.tsx` — 양쪽 동일

---

## 삭제된 파일

- 없음 (yohan-front에서 삭제한 파일 없음)
- `hooks/useRaspberryPiSimulator.ts`는 Capstone에서만 삭제 (yohan-front에 원래 없었음)

---

## 빌드 확인

- `npm run build` (vite build) 성공 ✅
- 두 폴더의 `src/` 완전 동일 확인 완료
