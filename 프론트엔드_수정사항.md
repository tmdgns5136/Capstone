# 프론트엔드 수정사항

## 1. 이의 신청 내역 드롭다운 표시
- [x] 이의 신청 내역 옆에 아래 화살표(▼) 추가해서 드롭다운이라는 것을 표현

## 2. 학생 정보 수정 취소 시 상태 초기화
- [x] 전화번호 등 수정 후 취소 버튼 누르면 원래 값으로 되돌려야 함 (현재는 수정한 상태 유지됨, 새로고침해야 원복)

## 3. 관리자용 학생/교수 추가 폼 초기화
- [x] 학생/교수 추가 중 다른 탭으로 이동했다 돌아오면 입력했던 정보가 유지되는 문제
- [x] 탭 전환 시 입력 폼 초기화되도록 수정

## 5. 학생 정보 조회 및 이의신청 오류
- [x] 202100002 로그인 시 학생 정보 안 뜨는 문제 확인 → catch 블록 에러 로깅/토스트 추가 (원인은 백엔드 응답 확인 필요)
- [x] 이의신청 시 "필수 항목 누락" 오류 원인 파악 → 세션 자동매칭 실패 시 sessionId가 NaN으로 전달됨. 모달에 세션 수동 선택 드롭다운 추가
- [x] 202100006 계정에서도 동일 증상 확인 → 동일 원인, 위 수정으로 커버됨

## 6. 공결신청 날짜 선택 불가
- [x] 날짜 선택이 안 되는 문제 확인 → 세션 빈 배열 시 "강의를 먼저 선택하세요" 표시되던 것을 "등록된 수업 날짜가 없습니다"로 개선 + 에러 로깅/토스트 추가 (근본 원인은 백엔드 세션 데이터 유무)

## 7. 관리자 알림 - 사진변경 요청 이동 문제
- [x] 테스트 데이터로 생성된 알림 클릭 시 사진변경 요청 페이지로 이동하지 않음 → 구/신 알림 타입 모두 지원하도록 매핑 확장 (PHOTO_RESULT + PHOTO 포함 타입 + 메시지 기반 fallback)
- [x] 직접 회원가입/사진변경 요청 시에는 정상 동작 → 테스트 데이터의 type 값이 다를 가능성 높음, 양쪽 모두 커버되도록 수정

## 8. Notification API 오류
- [x] 어떤 계정으로 로그인해도 F12 Network 창에 notification 관련 오류 발생 → 연속 3회 실패 시 폴링 자동 중단하여 Network 탭 오류 스팸 방지
- [x] 원인 파악 및 수정 → 첫 실패 시 console.warn 로깅 추가, 근본 원인은 백엔드 API 응답 확인 필요

## 9. 관리자용 시스템 개요 - 평균 출석률 제거
- [x] 시스템 개요 탭에서 평균 출석률 항목 제거 → 카드, 인터페이스, import 모두 정리, 그리드 3열로 변경

## 10. 관리자용 시스템 개요 - 인프라 상태 정리
- [x] 인프라 상태 항목이 무엇을 의미하는지 불명확 → 실제 모니터링이 아닌 중복 데이터이므로 섹션 전체 제거, 장치 상태 테이블은 유지

## 11. 하단 링크 페이지 추가
- [x] 개인정보처리방침 페이지 내용 추가 → /privacy 경로, 7개 조항
- [x] 이용약관 페이지 내용 추가 → /terms 경로, 7개 조항
- [x] 고객센터 페이지 내용 추가 → /support 경로, 연락처 카드 + FAQ 5개

## 12. 학생용 학기별 출결 요약 - 강의 상세 정보 개선
- [x] 강의 상세 정보에 담당 교수, 강의실 정보 추가 → 시간표 API에서 강의실 매칭
- [x] 과목 상세 출결 통계 제거 → "과목 상세 출결 통계" 텍스트 제거, 교수명/강의실로 대체

## 15. 학생용 홈 문구 수정
- [x] "수업 시간이 되면 실시간 출석 현황이 표시됩니다" → "수업 시간이 되면 실시간으로 내 상태가 표시됩니다"로 변경
- [x] 기타 홈 화면 문구 검토 → 나머지 문구 적절, 수정 불필요
